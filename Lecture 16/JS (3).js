// Получаем результат вычислений от пользователя
const result = +prompt("Введите результат (задуманное число × 5 + 7):");

// Вычисляем исходное число: (result - 7) / 5
const originalNumber = (result - 7) / 5;

// Выводим ответ с двумя знаками после запятой
alert(`Вы задумали число: ${originalNumber.toFixed(2)}`);

if (isNaN(result)) {
    alert("Нужно вводить числа!");
} else {
    // ... основной расчет ...
}