// Попросить пользователя заполнить массив 10-тью значениями
let arr = [];
for(let i=0; i<10; i++)
    {
        let element  = prompt("введите 10 массивов")
        arr.push(element)
    }
    console.log(arr)