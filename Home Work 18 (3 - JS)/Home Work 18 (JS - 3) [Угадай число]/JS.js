// Угадай число
let number;
do
{
    //Условие - Угадать число
    alert("Угадайте число от 1 до 20")
    number =+prompt("Число?")
}
while (!number || number < 0 || number > 20
               || number < 4 || numer > 4)

// Правильно число 4!